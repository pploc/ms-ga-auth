rootProject.name = 'ms-ga-auth'

// The shared platform library (delivery.md §10, ADR-0003 §4). Until a Maven registry exists it is
// consumed as a composite build: Gradle substitutes the com.gymapi:gymapi-java-common coordinate
// with the sibling checkout automatically.
includeBuild '../gymapi-java-common'
